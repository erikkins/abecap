---
name: session-progress
description: "Live session snapshot (auto-checkpointed ~15min) — what's done, in flight, and next"
metadata: 
  node_type: memory
  type: project
  originSessionId: 264056a8-f1e5-489c-9140-1fb57bda9825
---

# Session snapshot — thru Aug 24 2026

## Frozen spec
- "walk-forward" not "backtest"; never expose t30v/Core/Ensemble/DWAP PUBLIC; never "tape"; NO fabrication. Web deploy=push main→"Deploy RigaCap" GHA + smoke api.rigacap.com/api/market-data-status→200. Admin app=mobile-admin/ EAS OTA. NO Google Ads API (Erik does ads UI). RigaCap=PUBLISHER; 0 real external subs (the "6"=Erik+beta comps). perf_numbers SSOT. Verify before concluding; research maps to prod. Admin/test emails→erik@rigacap.com.

## 🧭 LIVE THREAD — FREE-FIRST conversion pivot (DESIGN done for free/paid line; not built) → full spec: [[project_free_first_spec]]
- WHY: 0 subs + unknown brand can't borrow trusted-incumbent (Fool/SA) card-friction → earn trust via FREE experience, card LATER. Ads bring OLD+MALE(55+≈65%) preservers who buy TRUST not FOMO; 83% mobile. Fixes the funnel leak (offer→CTA ~1/43 on /should-i-sell; hero/fold FINE at 48%).
- ✅ ALL 4 DECISIONS LOCKED: (1) FREE/PAID LINE = **PROOF ONLY, zero actionable** — free shows CLOSED-trade ledger (named results, reveal ON CLOSE: Maximizer ~29d, Preserver on-exit) + current book COUNTS/perf (NO names) + free market read (NO live tickers) + full track record; PAID = live names/entries/weights/exits/realtime. (2) Free-tier LEAD personalized by ad door + protect/grow toggle; tier CHOICE at upgrade only. (3) 30-day MONEY-BACK guarantee (replaces 7-day CC trial). (4) KEEP intro rate ($59→$129, 12mo lock) — reframe Founding/First-100 → "Introductory".
- KEY CATCH: daily market read NAMES live tickers → needs a FREE-tier ticker-free version; ANTI-LEAK generalizes to a PROSE-SURFACE audit (read/email/briefings), enforced at DATA layer (entitlement choke point → free never fetches live names) + fail-closed pre-send guard scanning free emails for ANY ticker.
- DATA MODEL: add `free` state, RETIRE `trial`, `is_valid()`=paid-only (active+grace); free users get explicit status='free' record at register; migration=non-event (comp internal/beta). QA: preview_state + force_state (parallels preview_tier/force_tier) to inspect every state's UI+email. Email capture (ANY point incl ABANDONED signup)→newsletter enroll (dedup by state, CAN-SPAM unsub, deliverability). Register→free (stop auto-Stripe); 2-step modal (card only at upgrade). Drip rewrite (free-nurture→paid, state-aware).
- BUILD PROGRESS: **#1 BACKBONE ✅ BUILT + DEPLOYED LIVE** (commits 640cfe1 + de05c7c import fix; Erik OK'd live testing "beta users won't login"). `Subscription.is_valid()`=paid-only + `.entitlement()` + `security.resolve_entitlement(user,sub)` choke point wired into `/dashboard` (route = **`/api/signals/dashboard`**, prefix /api/signals). Verified no-op on access (only 2 of 9 subs valid; **re-comped the 4 lapsed beta** to 2027-08-24 via run_migration UPDATE...RETURNING). **NO PICKLE — live=PARQUET/PITFWU** ([[feedback_no_pickle_parquet]], Erik emphatic).
- **#2 free payload DEPLOYED + verified live** (unauth GET /api/signals/dashboard → entitlement:'free'): plumbing all works — closed_ledger, open_book counts-only via new `tier_serving.tier_public_summary` (snapshot-only, price-source-agnostic), ticker-free `_build_free_market_read`, anti-leak scan clean. BUT verification exposed **anti-proof data + a values pivot (RESOLVING NOW)**:
  - closed_ledger source `get_signal_track_stats` (signal_track_record) = 0% win/−26% avg = UNREPRESENTATIVE artifact (unfiltered every-fresh-pick, flat-sized, NOT the model book). → switch to `cached['missed_opportunities']` (nightly WF **named winners**, already in cache, no backtest).
  - open_book `book_return_pct` = live-shadow **−8.2% Preserver / −1.6% Maximizer** (young ~2-mo June-launch drawdown).
  - free read printed broken `market_stats.spy_price=763` → drop absolute S&P (keep regime+VIX+activity).
- **#2 ✅ COMPLETE + DEPLOYED end-to-end (Aug24, honest composition per Erik).** Backend: closed_ledger=`cached['missed_opportunities']` (WF NAMED WINNERS — SNOW+22.5/ALAB+27.5/etc, NOT the signal_track 0%-win artifact); open_book counts+honest book_return_pct KEPT (Pres −8.2/Max −1.6, warts shown); market_read_free drops broken spy_price(763); **NEW dynamic `current_phase`** (`tier_serving.current_book_phase`) = honest "where we are right now" readout off Preserver book equity → live text "~2 months into a soft patch, 10.5% off its high...edge measured over full cycles" — AUTO-FLIPS to upswing when book recovers (Erik's idea). Frontend: **`components/FreeProofView.jsx`** (dynamic phase → 21yr record WITH drawdowns from perf_numbers → hold-now counts → named winners → ticker-free read → CTA w/ 30-day money-back) wired into App.jsx (replaced subscription_required teaser). All verified live at `/api/signals/dashboard` (unauth→entitlement:free), anti-leak clean, npm build clean.
  - ⚠️ **PREVIEW GAP:** Erik's admin=paid so he WON'T see FreeProofView normally; needs a lapsed/free acct (sarahshizas expired / erikkins@me.com now-invalid-trial) OR the #4 preview_state harness (not built). 
  - 📌 **START-DATE SWEEP finding (Erik asked what's publishable/feel-good):** recent 2.5mo Maximizer launch-sweep = all soft (median −2%, our Jun15 −8.7% worst) → NO feel-good timing story. BUT **CERTIFIED anchor (compute_tier_walkforward trailing-365): Preserver +67.8%/Sharpe2.08/−17.7%, Maximizer +54.2%/2.01/−13.4% vs SPY +20.5%** = the reconciliation ("engine's 12mo ~3× the market; live track just started mid-June at a dip"). CAVEATS before publishing: rolling-recent favorable window; differs from site's 24mo held-out (Max 48.9/yr) → RECONCILE first; "worst-entry-in-2yr / 92-95% start-dates positive" line is from STALE uncertified curve (recent_tier_curves.json, ends Jul9) → needs certified re-run.
- **#3 ✅ DEPLOYED register→free:** `auth._provision_free_account` (all 3 signup paths email/google/apple) creates status='free' sub + EmailSubscriber newsletter enroll (idempotent, never blocks). LoginModal reworked free-first: NO plan seeding (kills auto-Stripe), TWO-STEP (step1 email+OAuth→newsletter enroll for abandon-capture; step2 password+turnstile), "Credit card required"→"Free account · No credit card · 30-day money-back", copy "Create your free account". Weekly newsletter sends to EmailSubscriber (scheduler:2274).
- **#4 ✅ DEPLOYED QA harness:** dashboard `?preview_state=free|active|trial|expired|past_due|canceled` (ADMIN-gated — non-admin ignored so can't bypass paywall; 'active'→paid, else→free view), frontend forwards preview_tier+preview_state. **Email anti-leak = STRUCTURAL** (daily digest gate scheduler:1755 = is_valid()=paid-only → free users excluded, get only ticker-free newsletter). force_state email preview deferred to #6.
- 🚨 **OAUTH LOOP (acquaintance couldn't sign up — real prospect):** DB shows 0 users in 7d, beacon healthy (40/6h), 0 backend auth calls 2h → failure is CLIENT-SIDE OAuth handshake BEFORE account creation = likely Google/Apple provider config (authorized JS origins / redirect URIs / Apple Service ID + `/auth/apple/callback`) — ERIK TO CHECK CONSOLES. ALSO FIXED a separate post-account loop: `AuthContext.redirectToCheckoutIfNeeded` defaulted plan→'monthly' & auto-redirected every sub-less user to Stripe → now redirects ONLY on explicit plan (else free view). Commit df66b14. ADDED LOGGING (Erik "everything logged"): server prints on register/google/apple (entry/verified/new-vs-existing/turnstile-fail); client oauth_google_click/error + oauth_apple_click/error → page_views. NEXT if recurs: check page_views oauth_*_error + Lambda logs; video would pinpoint google-vs-apple.
- 🔥 **HOTFIX 950268a (email signup was BROKEN since #3):** my #3 edit inserted `_provision_free_account` BETWEEN `@router.post('/register')` and `register()` → decorator bound to the helper (bare db,user args → FastAPI 422 "missing query db/user"); real register() had NO route. Every EMAIL signup 422'd since #3 (309abd3) → UI bounced to signup screen (what Erik hit). FIXED: decorator back on register(), helper is plain module fn. VERIFIED live: POST /register → 400 turnstile (was 422) + `🔑 [SIGNUP]` log fires. OAuth routes were unaffected. LESSON: never insert a fn between a route decorator and its handler.
- **JACOB (acquaintance) vs the #3 bug:** DIFFERENT bugs. Jacob hit OAuth (google/apple), NOT the email-route 422. His exact failure predates logging → still uncaptured. Told Erik honestly: we fixed 2 real signup bugs today (email 422 + checkout loop) but haven't captured Jacob's specific one. Jacob to RETRY (now fully instrumented) → check page_views oauth_*_error + Lambda 🔑[OAUTH]; video would pinpoint. Existing test users: erikkins+incognitojoe@gmail.com (Jul10), +test (Jun23) — +alias works for EMAIL signup, NOT Google login (alias≠real Google account = "couldn't find account", expected).
- 🧯 **SIGNUP-FLOW FIREFIGHT (Erik live walkthrough, all DEPLOYED):** cascade found+fixed: (1) email 422 decorator bug [950268a]; (2) auto-Stripe ~10s after login — RETIRED both App.jsx auto-checkout effect (now just clears stale rigacap_selected_plan on mount) + AuthContext.redirectToCheckoutIfNeeded (clears keys, always returns false; NEVER auto-redirects — upgrade is explicit-click only) [a903166]; (3) back-from-Stripe /pricing 404 — billing.py success/cancel_url /dashboard+/pricing (NOT SPA routes → client 404) → both now /app [a903166]; (4) free view was crammed in old PAID 2-col shell ($0 portfolio, empty Open Positions, PRESERVER badge, "what to expect" bar) → gated ALL paid chrome on !subscription_required (metrics strip, 2-col→1-col, buy-signals header, Open Positions col, regime bar ×2, tier badge) [d68607e]. FreeProofView itself renders great (verified via screenshot).
- 🔄 **MAJOR MODEL PIVOT (Aug 24, Erik decided): FREE = no-card 30-day FULL-PRODUCT TRIAL → proof floor.** He felt proof-only was "marketing veneer" that won't convert; wants them to USE the real product. NEW lifecycle: signup(no card)→`trial` 30d (FULL dashboard, live signals, BOTH tiers, regime control)→trial_end→PROOF FLOOR (FreeProofView, NOT lockout)→upgrade to `active` (paid+30-day money-back). SHIPPED [3f59bbb, 66c3124]: is_valid() re-includes no-card trial within trial_end (reverses backbone's exclude-trial); `_provision_free_account` now creates status='trial' trial_end=+30d (all 3 signup paths); **compmax LEAK FIXED** — trial grants both tiers via NEW `Subscription.has_maximizer_access()` (=has_maxpp_addon OR compmax OR valid-trial, trial-aware/non-persistent, used in to_dict+tier_serving.resolve_tier+scheduler._is_max) so converting to Preserver-only can't leak free Maximizer; trial no longer sets compmax. Regime control UN-hidden for proof floor (Erik). **VERIFIED: fresh email signup erikkins+login2@gmail.com → trial 30d, full product works.** (that acct has stale compmax=True — signed up pre-fix; harmless test.)
- 🩹 **LIVE-WALKTHROUGH PUNCH-LIST (Erik testing fresh signups; all SHIPPED):** OAuth/checkout LOOP fixed (redirectToCheckoutIfNeeded no longer defaults plan→monthly/auto-redirects; App.jsx auto-checkout effect RETIRED→just clears stale plan) + full OAuth logging (server 🔑[OAUTH]/[SIGNUP] + client oauth_*_click/error); Stripe success/cancel_url /dashboard+/pricing (non-SPA→client 404) → both /app; free view OWNS layout (paid chrome all gated on !subscription_required); inner 500px scroll removed for free; books px-4 inset removed so they align w/ top cards (= the "regime wider than books" complaint, FIXED); `<main>` pb-28 so cookie banner doesn't overlay last card; **Founder→Introductory reframe on LandingPageV2** (dropped "First 100"/seats FOMO counter, CTAs→"Start free — no card", kept "Direct line to founder" adviser-only + intro $59→$129/12mo-lock + founding-status `open` flag). Verified: fresh email signup → 30d trial → straight into dashboard, no funnybusiness.
- ✅ **FLASH REFACTOR DONE [3e05294]:** every free-vs-full layout branch now keys off a single STABLE `freeTier` (App.jsx ~1929) = `isAdmin ? subscription_required===true : (isAuthenticated && !hasValidSubscription)` — resolved from /me BEFORE the dashboard fetch, so correct layout paints frame-one regardless of stale/cross-account cache. Replaced ALL `dashboardData?.subscription_required` layout checks with `freeTier`. Admin ?preview_state=free still works (admin path reads payload flag). Did NOT physically hoist paid JSX (still gated in-tree) — fixed the CAUSE (late branch signal) not the structure; offered loader-until-fresh if any flash remains. Also removed 2 landing bullets (Direct reply/Grandfathered) [a4de99f].
- ⏳ **STILL QUEUED:** (2) CTA dynamic pricing — FreeProofView "$1,099" btn + SubscriptionBanner → live intro price from /api/billing/founding-status (returns {taken,limit,remaining,open} — NO prices yet; add price fields as SSOT). (3) DRIP rewrite (#6) — re-key `scheduler.send_onboarding_drip_emails` (days_since created_at + onboarding_step) to the 14-day window: D1-3 how-to-act, ~D12-13 "access winds down, subscribe", D15+ proof-floor conversion; nightly digest + sell-alerts ALREADY auto-phase via is_valid (structural anti-leak — free never gets live tickers); weekly newsletter unchanged; add fail-closed pre-send guard. (4) residual "founding" copy sweep (ShouldISell/Momentum/blogs). (5) SubscriptionBanner.jsx:24 plan→'monthly' default (explicit-click only, low pri).
- **NEXT (free-first remaining):** flash refactor (above) → #5 upgrade/offer copy + Introductory reframe → #6 drip rewrite + fail-closed email guard + force_state → #7 door/landing copy. Jacob's OAuth loop still uncaptured (retry now instrumented: page_views oauth_*_error + Lambda 🔑[OAUTH]). Optional: reconcile trailing-365 (+54/+68 vs SPY+20.5) for free-view publishing; backfill free subs for pre-#3 sub-less accounts.

## ✅ SHIPPED recently (live): Maximizer vol-brake cold-start fix (bb3c9be, self-heals Aug24 scan); start-date sweep (c622e3e); newsletter weekly-pack+regime-run+tier rule (f127d0b); SPY-trend reads +3streak; 2 ad doors + dual funnel tracking + signup sub-steps; personal social posts; "Pascal's Portfolio" newsletter draft seeded ([[project_newsletter_pascal_topic]]).

## ▶ OTHER OPEN: ad negatives to paste (marketbeat/warrior trading/trendspider/etc); DOCS refresh (design/documents/* uncommitted); verify brake gauge after Aug24 4pm scan.