# Copy this file to terraform.tfvars and fill in your values
# terraform.tfvars should be in .gitignore (secrets!)

db_password           = "YourSecureDbPassword123!"
jwt_secret_key        = "your-generated-jwt-secret-from-openssl"

# Stripe configuration
stripe_secret_key     = "sk_live_..."
stripe_webhook_secret = "whsec_..."
stripe_price_id       = "price_..."           # Monthly price ID
stripe_price_id_annual = "price_..."          # Annual price ID

# Cloudflare Turnstile
turnstile_secret_key  = "0x..."

# Gmail SMTP for email sending
# Create an App Password at: https://myaccount.google.com/apppasswords
smtp_user             = "daily@rigacap.com"   # Also used as FROM_EMAIL
smtp_pass             = "xxxx xxxx xxxx xxxx" # 16-char App Password

# Admin emails - only these addresses receive internal notifications
admin_emails          = "erik@rigacap.com"    # Comma-separated for multiple
