# Core
fastapi==0.109.0
pydantic==2.5.0
pydantic-settings==2.1.0
mangum==0.17.0

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2

# Payments
stripe==7.10.0

# Database
sqlalchemy==2.0.25
asyncpg==0.29.0
greenlet==3.0.3

# Data (yfinance and minimal deps)
yfinance>=1.1.0
pandas==2.1.4
numpy==1.26.3

# Scheduling
apscheduler==3.10.4
pytz==2024.1

# Utilities
python-dotenv==1.0.0
httpx==0.26.0
aiohttp==3.9.1
email-validator==2.1.0

# Note: Excluded for Lambda (too large or not needed):
# - boto3, botocore (already in Lambda runtime)
# - pyarrow (too large, not essential)
# - uvicorn, uvloop (not needed with Mangum)
# - pytest, pytest-asyncio (testing only)
# - alembic (migrations run separately)
